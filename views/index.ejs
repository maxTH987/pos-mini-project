<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ระบบ POS Mini Project</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>

<body class="bg-light">

    <%- include('navbar') %>

    <div class="container mt-4">
        <div class="row">
            
            <div class="col-md-7">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0"><i class="fa-solid fa-border-all me-2"></i>เลือกสินค้าที่ต้องการ</h5>
                    </div>
                    <div class="card-body bg-light p-3" style="max-height: 75vh; overflow-y: auto;">
                        
                        <div class="row row-cols-2 row-cols-md-3 g-3">
                            <% if (products && products.length > 0) { %>
                                <% products.forEach(function(product) { %>
                                    <div class="col">
                                        <div class="card h-100 shadow-sm border-0" style="transition: 0.3s; cursor: pointer;" onmouseover="this.style.transform='scale(1.03)'" onmouseout="this.style.transform='scale(1)'">
                                            <img src="<%= product.imageUrl || 'https://placehold.co/200x200?text=No+Image' %>" class="card-img-top p-2" alt="Product" style="height: 120px; object-fit: contain;">
                                            
                                            <div class="card-body text-center p-2">
                                                <h6 class="card-title mb-1 text-truncate" title="<%= product.name %>"><%= product.name %></h6>
                                                <p class="card-text text-primary fw-bold mb-2"><%= product.price %> ฿</p>
                                                
                                                <% if(product.stock > 0) { %>
                                                    <button class="btn btn-sm btn-outline-primary w-100 fw-bold" 
                                                        onclick="addToCart('<%= product._id %>', '<%= product.name %>', <%= product.price %>, <%= product.stock %>)">
                                                        + เพิ่ม (<%= product.stock %>)
                                                    </button>
                                                <% } else { %>
                                                    <button class="btn btn-sm btn-danger w-100" disabled>หมดสต๊อก</button>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div class="col-12 text-center text-muted py-5 w-100">
                                    <h4><i class="fa-solid fa-box-open mb-3"></i></h4>
                                    ยังไม่มีสินค้าในระบบ
                                </div>
                            <% } %>
                        </div>
                        
                    </div>
                </div>
            </div>

            <div class="col-md-5">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0"><i class="fa-solid fa-receipt me-2"></i> ตะกร้าสินค้า</h5>
                    </div>
                    <div class="card-body bg-white">
                        <ul id="cart-items" class="list-group mb-3">
                            <li class="list-group-item text-center text-muted border-0">ตะกร้าว่างเปล่า</li>
                        </ul>
                        <hr>
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h5 class="mb-0">ยอดรวม:</h5>
                            <h3 class="text-danger mb-0"><span id="total-price">0</span> บาท</h3>
                        </div>

                        <div id="promo-text" class="text-center mb-2"></div>

                        <div class="input-group mb-3">
                            <span class="input-group-text bg-light">ส่วนลดโปรโมชั่น</span>
                            <input type="number" id="discount" class="form-control" value="0" readonly>
                        </div>

                        <div class="input-group mb-3">
                            <span class="input-group-text bg-light">รับเงินมา (บาท)</span>
                            <input type="number" id="amount-paid" class="form-control" placeholder="0">
                        </div>

                        <div class="input-group mb-4">
                            <span class="input-group-text bg-light"><i class="fa-solid fa-user-tag me-1"></i> เบอร์สมาชิก</span>
                            <input type="text" id="member-phone" class="form-control" placeholder="ใส่เบอร์เพื่อสะสมแต้ม (ไม่บังคับ)">
                        </div>
                        <button class="btn btn-success w-100 btn-lg shadow-sm" onclick="checkout()">
                             ชำระเงิน
                        </button>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/script.js"></script>

</body>
</html>